/* =============================================================
   إعدادات نظام بيان لإدارة المهام واتفاقية مستوى الخدمة
   -------------------------------------------------------------
   هذا هو الملف الوحيد الذي تحتاج تعديله لربط النظام بقاعدة
   البيانات المشتركة (Supabase).

   • اتركه فارغاً  ←  يعمل النظام في «الوضع التجريبي» على جهازك
                      فقط (تخزين محلي في المتصفح) بلا أي إعداد.
   • عبّئ القيمتين ←  يتحول النظام تلقائياً إلى قاعدة البيانات
                      المشتركة ويرى الجميع نفس البيانات.

   من أين تجلب القيمتين؟ راجع «دليل_التشغيل.md» الخطوة ٣.
   ============================================================= */

const CONFIG = {
  /* --- ١. ربط قاعدة البيانات المشتركة --- */
  SUPABASE_URL: "",       // مثال: "https://abcdefghijk.supabase.co"
  SUPABASE_ANON_KEY: "",  // المفتاح العام (anon public) — وليس service_role

  /* --- ٢. بيانات الجمعية --- */
  ORG_NAME: "الجمعية الخيرية لتحفيظ القرآن الكريم بمحافظة رماح — بيان",
  ORG_SHORT: "جمعية بيان",
  LOGO_PATH: "assets/logo.png",

  /* --- ٣. قواعد اتفاقية مستوى الخدمة --- */
  DEFAULT_SLA_DAYS: 5,      // المدة الافتراضية بأيام العمل
  WORK_DAYS: [0, 1, 2, 3, 4], // الأحد=0 … الخميس=4 (الجمعة والسبت عطلة)
  WORK_START_HOUR: 8,       // ٨:٠٠ صباحاً
  WORK_END_HOUR: 16,        // ٤:٠٠ عصراً
  CUTOFF_HOUR: 14,          // الطلب بعد ٢:٠٠ ظهراً يُحتسب من يوم العمل التالي
  URGENT_FACTOR: 0.5,       // الطلب العاجل = نصف المدة (يُقرَّب لأعلى)

  /* --- ٤. عتبات ألوان مؤشر الالتزام --- */
  THRESHOLD_OK: 90,         // ٩٠٪ فأعلى  → أخضر
  THRESHOLD_WARN: 75,       // ٧٥٪ – ٨٩٪  → أصفر · أقل من ٧٥٪ → أحمر

  /* --- ٥. التصعيد عند التأخر (بأيام العمل بعد تاريخ الاستحقاق) --- */
  ESCALATION: [
    { days: 1, to: "المنفّذ المكلَّف", label: "تذكير آلي" },
    { days: 2, to: "مدير الإدارة", label: "تصعيد أول" },
    { days: 3, to: "الرئيس التنفيذي", label: "تصعيد ثانٍ" },
  ],
};

/* الوضع الحالي — يُحدَّد آلياً، لا تعدّله */
CONFIG.MODE = (CONFIG.SUPABASE_URL && CONFIG.SUPABASE_ANON_KEY) ? "supabase" : "demo";
