# دليل تشغيل نظام بيان لإدارة المهام

نظام إدارة مهام ومتابعة الالتزام باتفاقية مستوى الخدمة، بتسعة أقسام وثلاثة مستويات صلاحية.

---

## أولاً: جرّبه الآن — بلا أي إعداد

انقر نقراً مزدوجاً على **`index.html`**. يفتح النظام في المتصفح ويعمل مباشرة في «الوضع التجريبي»، وفيه:

- 9 أقسام · 74 خدمة · أكثر من 120 مهمة نموذجية موزَّعة على خمسة أشهر
- 21 حساباً تجريبياً — اضغط أي حساب في شاشة الدخول ليدخل فوراً
- كلمة المرور الموحّدة: **`bayan1446`**

جرّب هذه الحسابات لترى الفروق بين الصلاحيات الثلاث:

| الحساب | الدور | ماذا يرى |
|---|---|---|
| `owner@bayan.sa` | المالك | كل الأقسام، لوحة الإنجاز والتأخر الشاملة، الإعدادات |
| `qa.manager@bayan.sa` | مدير قسم | قسم الجودة فقط، وأداء موظفيه، وصندوق قراراته |
| `tech@bayan.sa` | موظف | مهامه المسندة وطلباته المرفوعة فقط |

> **حدّ الوضع التجريبي:** البيانات محفوظة في متصفح جهازك وحده. المهمة التي ترفعها هنا لا تصل لجهاز زميلك. لتشغيل النظام على الجمعية كلها، أكمل الخطوات التالية.

---

## ثانياً: التشغيل الحقيقي على قاعدة بيانات مشتركة

الوقت المتوقع: 15–20 دقيقة، ومجاناً على خطة Supabase المجانية.

### الخطوة 1 — أنشئ مشروع Supabase

1. افتح [supabase.com](https://supabase.com) وسجّل دخولك (يمكن بحساب Google).
2. اضغط **New project**.
3. عبّئ:
   - **Name:** `bayan-tasks`
   - **Database Password:** كلمة مرور قوية — **احفظها في مكان آمن**، لن تظهر مرة أخرى.
   - **Region:** اختر `Central EU (Frankfurt)` أو أقرب منطقة — الأسرع استجابةً للسعودية.
4. اضغط **Create new project** وانتظر 2–3 دقائق حتى يجهز.

### الخطوة 2 — أنشئ الجداول والصلاحيات

1. من القائمة الجانبية اختر **SQL Editor** ← **New query**.
2. افتح ملف `supabase/schema.sql` بأي محرر نصوص، وانسخ **كامل** محتواه والصقه.
3. اضغط **Run**. المتوقع: `Success. No rows returned`.
4. كرّر نفس الخطوات مع ملف `supabase/seed.sql`.

للتأكد من نجاح الخطوة، شغّل هذا الاستعلام في نافذة جديدة:

```sql
select
  (select count(*) from public.departments) as أقسام,
  (select count(*) from public.services)    as خدمات,
  (select count(*) from public.holidays)    as عطل;
```

المتوقع: **9 أقسام · 74 خدمة · 6 عطل**.

### الخطوة 3 — انسخ المفتاحين إلى النظام

1. من القائمة الجانبية: **Project Settings** ← **API**.
2. انسخ القيمتين:
   - **Project URL** — يشبه `https://abcdefghijk.supabase.co`
   - **anon public** — مفتاح طويل يبدأ بـ `eyJ...`

   ⚠️ **لا تنسخ مفتاح `service_role` أبداً.** ذلك المفتاح يتجاوز كل الصلاحيات، ووضعه في ملف يفتحه المتصفح يعني أن أي موظف يستطيع قراءة وتعديل كل شيء.

3. افتح `js/config.js` بأي محرر نصوص، وضع القيمتين بين علامتَي التنصيص:

```js
SUPABASE_URL: "https://abcdefghijk.supabase.co",
SUPABASE_ANON_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
```

4. احفظ الملف. عند فتح النظام ستجد الوسم في الأعلى تحوّل من «وضع تجريبي» إلى **«قاعدة بيانات مشتركة»**.

### الخطوة 4 — أنشئ حسابك كمالك

الحسابات في النظام الحقيقي تُنشأ بالتسجيل الذاتي ثم التفعيل. أول حساب هو حسابك:

1. افتح النظام واضغط **«حساب جديد»**.
2. سجّل باسمك وبريدك وكلمة مرور.
3. عد إلى لوحة Supabase ← **SQL Editor**، وشغّل هذا الأمر بعد وضع بريدك:

```sql
update public.profiles
set role = 'owner', active = true, job_title = 'الرئيس التنفيذي'
where email = 'ضع-بريدك-هنا@example.com';
```

4. سجّل الدخول في النظام — ستجد تبويب **«⚙️ الإعدادات والمستخدمون»** ظهر لك.

هذه هي المرة **الوحيدة** التي تحتاج فيها SQL لإدارة المستخدمين. بعدها تدير الجميع من داخل النظام.

### الخطوة 5 — أضف الموظفين

1. أرسل رابط النظام لكل موظف واطلب منه التسجيل بزر **«حساب جديد»**.
2. يظهر الحساب عندك في **الإعدادات ← المستخدمون** بحالة **«معطَّل»**.
3. اضغط **تعديل** لكل حساب، واضبط: الدور (مدير إدارة / موظف)، والإدارة، والمسمّى الوظيفي، ثم فعّله.

> **لماذا لا تُنشئ الحسابات بنفسك من النظام؟** لأن إنشاء حساب يتطلب مفتاح `service_role` السرّي، ووضعه في المتصفح ثغرة أمنية حقيقية. التسجيل الذاتي ثم التفعيل يعطي نفس النتيجة بأمان.
>
> **الحذف النهائي** للحساب يتطلب المفتاح نفسه، لذلك **يُعطَّل** الحساب بدل حذفه — والتعطيل يمنع الدخول فعلياً ويُبقي سجل المهام سليماً للتقارير.

### الخطوة 6 — ارفع النظام على الإنترنت (اختياري)

يمكن للموظفين فتح الملفات من جهاز مشترك، لكن الأسهل رفعه على رابط:

**الطريقة الأسرع — Netlify Drop (مجاناً وبلا حساب):**

1. افتح [app.netlify.com/drop](https://app.netlify.com/drop).
2. اسحب مجلد المشروع كاملاً وأفلته في الصفحة.
3. تحصل على رابط مثل `https://bayan-tasks.netlify.app` — أرسله للموظفين.

> عند تحديث أي ملف، أعد سحب المجلد لتحديث الموقع.

---

## بنية الملفات

```
index.html              الواجهة الكاملة
assets/style.css        هوية بيان البصرية
assets/logo.png         الشعار

js/config.js            ← الملف الوحيد الذي تعدّله للربط
js/seed-data.js         الأقسام والخدمات والقواعد (مصدر البذور)
js/sla.js               محرّك أيام العمل والالتزام والألوان
js/workflow.js          دورة حياة المهمة والصلاحيات على الإجراءات
js/db-demo.js           طبقة البيانات — الوضع التجريبي
js/db-supabase.js       طبقة البيانات — قاعدة البيانات المشتركة
js/ui.js                أدوات الواجهة المشتركة
js/app.js               النواة والتنقّل وشاشة الدخول
js/views/               الشاشات: اللوحة · المهام · الخدمات · التقارير · الإعدادات

supabase/schema.sql     الجداول والدوال وسياسات الحماية (RLS)
supabase/seed.sql       الأقسام والخدمات والعطل والحسابات التجريبية
```

---

## كيف يعمل النظام

### مسار الطلب — المدة **لا** تُحتسب فيه

1. الموظف يختار القسم والخدمة، ويؤكّد اكتمال المتطلبات، ويرفع المرفقات
2. الإرسال ← يُولَّد رقم متابعة (`BN-2026-0042`)
3. مدير القسم المستقبِل يدقّق اكتمال الطلب
4. **إما** يعيده لنقص بيانات (المدة لم تبدأ، ولا يُحتسب تأخر على القسم)
   **أو** يقبله ← **هنا فقط يبدأ العدّ**

### مسار التنفيذ — المدة تُحتسب من لحظة القبول

5. قبول الطلب ← يُثبَّت تاريخ الاستحقاق ولا يتغيّر بعدها
6. إسناد لموظف مختص
7. التنفيذ
8. رفع المخرجات لاعتماد المدير
9. اعتماد المدير ← يُقارَن وقت الإنجاز بتاريخ الاستحقاق
10. مقدّم الطلب يستلم ويقيّم ← إغلاق

**الفصل بين المسارين هو جوهر الاتفاقية:** لا يُحاسَب القسم على تأخير سببه طلب ناقص.

### احتساب المدة

- **أيام العمل:** الأحد – الخميس. الجمعة والسبت والعطل الرسمية المسجّلة مستثناة.
- **المدة الافتراضية:** 5 أيام عمل. والاستثناءات لكل خدمة (يوم واحد لبلاغ العطل التقني، 15 يوماً لدراسة فرصة استثمارية…).
- **ساعة القطع:** الطلب المقبول بعد الساعة 2:00 ظهراً يُحتسب من يوم العمل التالي.
- **الطلب العاجل:** نصف المدة، يُقرَّب لأعلى (5 أيام ← 3).

مثال: طلب قُبل الأربعاء 10 صباحاً بمدة 5 أيام → يستحق **الثلاثاء** التالي (الأربعاء، الخميس، ثم الأحد والإثنين والثلاثاء).

### مؤشر الالتزام وألوانه

**نسبة الالتزام** = المهام المنجزة في موعدها ÷ (المنجزة + المتأخرة) × 100

| النسبة | اللون | الدلالة |
|---|---|---|
| 90% فأعلى | 🟢 أخضر | ملتزم |
| 75% – أقل من 90% | 🟡 أصفر | تحت المتابعة |
| أقل من 75% | 🔴 أحمر | متعثر — يستدعي خطة تصحيحية |

ويُعرض بجانبه **معدل الإنجاز** = المنجز ÷ إجمالي المهام. المؤشران منفصلان ولا يُخلطان: قسم قد ينجز كل مهامه لكن متأخراً.

### التصعيد عند التأخر

| التجاوز | التصعيد |
|---|---|
| يوم عمل واحد | تذكير للمنفّذ المكلَّف |
| يوما عمل | مدير الإدارة |
| 3 أيام عمل فأكثر | الرئيس التنفيذي |

يظهر مستوى التصعيد في جدول «المهام المتأخرة» وفي صفحة كل مهمة.

---

## الصلاحيات

| | المالك | مدير قسم | موظف |
|---|:---:|:---:|:---:|
| رؤية كل الأقسام | ✅ | ❌ | ❌ |
| رؤية قسمه كاملاً | ✅ | ✅ | ❌ |
| رؤية مهامه وطلباته | ✅ | ✅ | ✅ |
| رفع طلب لأي قسم | ✅ | ✅ | ✅ |
| فرز وقبول ورفض الطلبات | ✅ | قسمه فقط | ❌ |
| إسناد المهام واعتماد الإنجاز | ✅ | قسمه فقط | ❌ |
| مراقبة أداء الموظفين | ✅ | قسمه فقط | ❌ |
| إدارة المستخدمين والخدمات والعطل | ✅ | ❌ | ❌ |

الصلاحيات مطبَّقة **مرتين**: في الواجهة (إخفاء ما لا يخصّه)، وفي قاعدة البيانات عبر سياسات RLS. الثانية هي الحماية الحقيقية — لو تلاعب موظف بشيفرة الصفحة، لن تُرجع له القاعدة صفاً واحداً لا يملكه.

كذلك **لا توجد سياسة تعديل مباشر على جدول المهام إطلاقاً**: كل تغيير في الحالة يمر عبر دالة `task_action` التي تتحقق من الصلاحية وتكتب السجل. فلا يستطيع أحد — ولا المدير — تعديل تاريخ استحقاق أو حالة يدوياً ليُحسّن مؤشره.

---

## أسئلة شائعة

**كيف أغيّر مدة خدمة؟**
الإعدادات ← الخدمات والمدد ← تعديل. التغيير يسري على **الطلبات الجديدة فقط**؛ الطلبات القائمة تحتفظ بالمدة التي قُبلت بها حتى لا تتغيّر نتائج القياس بأثر رجعي.

**كيف أضيف عطلة رسمية؟**
الإعدادات ← العطل الرسمية. سجّلها **مسبقاً** — الأيام المسجّلة لا تُحتسب في المدة، والتسجيل المتأخر لا يعيد حساب المهام المقبولة قبله.

**أين أرقام الأداء الشهرية؟**
تبويب «تقارير الأداء»: حدّد الفترة والقسم، ثم اطبع (PDF) أو صدّر CSV.

**كيف آخذ نسخة احتياطية؟**
الإعدادات ← النسخ الاحتياطي ← تصدير نسخة JSON. وفي Supabase تُؤخذ نسخ يومية تلقائياً من `Database ← Backups`.

**ظهر لي «تعذّر الاتصال بقاعدة البيانات».**
تحقّق من: الاتصال بالإنترنت · صحة الرابط والمفتاح في `js/config.js` · أن مشروع Supabase غير موقوف (المشاريع المجانية تُوقَف بعد أسبوع بلا استخدام، ويعاد تشغيلها بزر من اللوحة).

**موظف سجّل ولا يستطيع الدخول.**
حسابه لم يُفعَّل بعد. الإعدادات ← المستخدمون ← تعديل ← اضبط الدور والإدارة وفعّله.

---

*نظام بيان — «خيركم من تعلّم القرآن وعلّمه»*
